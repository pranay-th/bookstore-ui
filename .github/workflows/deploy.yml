name: Frontend Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to Vercel
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Add Vercel CLI deploy step
      # TODO: Add VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID secrets
      - name: Placeholder deploy step
        run: echo "TODO - wire up Vercel deployment secrets and CLI"
